# 本地编辑模式使用说明

## 快速开始

1. **双击 `edit-mode.bat`** 
2. 等待浏览器自动打开
3. 开始编辑！

就这么简单！

---

## 工作原理

### 第一次运行
- `edit-mode.bat` 自动生成密钥文件 `.env.local`
- 密钥文件保存在你的电脑上，**永不上传到 GitHub**
- 启动开发服务器并打开编辑界面

### 后续运行
- 直接使用已有的密钥
- 无需验证码，立即进入编辑模式

---

## 安全保障

✅ **线上完全安全**
- 部署到服务器的代码**不包含**编辑功能
- 任何人访问线上网站都**无法编辑**

✅ **本地密钥保护**
- `.env.local` 文件包含在 `.gitignore` 中
- 即使别人克隆你的代码，也没有你的密钥文件
- 只有你的电脑能编辑

✅ **代码仓库安全**
- `edit-mode.bat` 也被排除在 Git 之外
- GitHub 上看不到任何敏感信息

---

## 文件说明

### 本地文件（不会上传）
- `edit-mode.bat` - 启动脚本
- `.env.local` - 密钥文件（自动生成）

### 可以上传的文件
- `.env.local.example` - 配置模板（供参考）
- 修改后的代码文件

---

## 编辑功能

通过本地编辑模式，你可以：

✅ 修改音乐制作人介绍
✅ 编辑专辑乐评
✅ 更新书籍评论
✅ 编辑游戏评价
✅ 修改动漫推荐
✅ 更新所有文字内容

修改会自动保存到本地文件，然后你可以：
1. 用 Git 提交修改
2. 推送到 GitHub
3. 部署到线上

---

## 常见问题

### Q: 密钥文件丢失了怎么办？
A: 再次双击 `edit-mode.bat`，会自动生成新的密钥文件。

### Q: 可以在其他电脑上编辑吗？
A: 可以！把整个项目文件夹（包括 `edit-mode.bat`）复制到另一台电脑，双击 bat 文件即可。会在那台电脑上生成新的密钥。

### Q: 线上网站会有编辑功能吗？
A: 不会！线上完全没有编辑入口，只有通过本地开发环境才能编辑。

### Q: 别人克隆我的代码能编辑吗？
A: 不能！他们没有你的 `.env.local` 密钥文件，而且 `edit-mode.bat` 也不会被上传。

### Q: 需要更新密钥吗？
A: 不需要！密钥永久有效，除非你手动删除 `.env.local` 文件。

---

## 技术细节

编辑权限验证逻辑：
```
必须同时满足：
1. 运行在 localhost（本地开发）
2. URL 包含 ?editMode=local 参数
3. 存在 .env.local 文件且 VITE_EDIT_MODE=true
```

任何一个条件不满足，都无法进入编辑模式。

---

## 维护

### 如果需要重置
1. 删除 `.env.local` 文件
2. 双击 `edit-mode.bat` 重新生成

### 如果需要分享给协作者
只需分享项目代码，他们双击 `edit-mode.bat` 会自动生成自己的密钥。

---

**享受轻松的本地编辑体验！** 🎉
